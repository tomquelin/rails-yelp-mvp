<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <h1>Our Restaurants</h1>
      <ul>
        <% @restaurants.each do |restaurant| %>
          <li><%= restaurant.name %>  <%= link_to "(Read more)", restaurant_path(restaurant)%></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
